# プライバシーポリシー — speech-popup

[English](PRIVACY.md) · 更新日: 2026年9月7日

speech-popup は takeshy が開発するデスクトップ音声入力アプリです。この文書はアプリの動作を説明します。選択した音声認識サービスには、その提供者の利用規約・プライバシーポリシーが別途適用されます。

## マイクと音声認識

録音・ブラウザ音声認識を開始するとマイクを使用します。既定ではポップアップを開くと録音を開始しますが、設定で無効化できます。窓を隠すとマイクの取得を停止します。

録音方式では音声を WAV に変換し、コンピューターから選択した OpenAI・OpenAI 互換・whisper.cpp・Gemini・Vertex AI の接続先へ直接送信します。ローカル接続先ならそのサーバーまで、クラウドやリモート接続先ならそのサービスへ送信します。ブラウザ音声認識は WebView/ブラウザ提供者の機能を使い、外部サービスへ音声を送信する場合があります。

開発者が運営する認識サーバーや中継サーバーはありません。提供者によるデータの保存・処理は各サービスのポリシーに従います。クラウドサービスには別のアカウントや利用料金が必要な場合があります。

認識に失敗した音声は再送用にメモリに保持します。アプリ自身は音声ファイルとして保存しません。認識成功・明示的な破棄・一部のセッションや方式の変更・アプリ終了で破棄されます。WebView・OS・認識サービスの動作はこの保持機構の範囲外です。

## クリップボードと履歴

コピー操作は認識結果をシステムのクリップボードに入れます。自動貼り付けが有効なら、直前のアプリへテキストを貼り付けます。自動貼り付けは設定で無効化できます。

ポップアップを開くたびに現在のクリップボードのテキストを読み取り、ローカル履歴に加えます。他のアプリでコピーした機密情報が含まれる場合があります。履歴は最大30件、重複を除いて `history.json` に保存します。履歴をアップロードしません。次回ポップアップを開いた際、未コピーの下書きも履歴へ保存して入力欄を空にします。コピーも次回表示もしていない下書きはメモリにのみ保持し、終了後には復元しません。

## 設定・認証情報・ログ

設定と API Key は `config.toml`、Google OAuth クライアント情報・リフレッシュトークン・アクセストークン・有効期限は `credentials/vertex-oauth.json` に保存します。暗号化はしていません。対応する Unix 環境では制限付きのファイルモードで作成し、Windows では保存先のユーザープロファイルとアクセス権に従います。

Google OAuth は Vertex AI リクエストの認証に使用します。接続解除時はリフレッシュトークンの失効を試み、ローカルの認証情報を削除します。失効処理に失敗した場合は Google アカウント側でもアクセス権を解除できます。

診断ログには起動・ホットキー・操作上のエラーを記録します。音声・認識結果・API Key を意図的には記録しませんが、外部からのエラーメッセージに追加情報が含まれる可能性があります。共有前に内容を確認してください。

## 解析とユーザーによる管理

広告・利用状況解析 SDK は含まず、ユーザーデータを販売しません。外部サービスへの接続は利用する機能に伴って行います。

ローカルデータを削除するにはアプリを終了し、履歴・設定・認証情報・ログを削除してください。設定の情報欄と README にパスを示しています。Store 版は Windows のパッケージ仮想化によりパッケージのプロファイル配下に保存される場合があります。ローカル削除は、サービス提供者・クリップボード管理アプリ・貼り付け先が保持するデータの削除にはなりません。

設定から自動録音・自動貼り付けを無効化できます。OS でマイク権限を拒否でき、Windows の「設定 → アプリ → スタートアップ」から自動起動を無効化できます。

## お問い合わせと更新

お問い合わせは [プロジェクトの Issue](https://github.com/takeshy/speech-popup/issues) へお願いします。API Key・トークン・機密の認識結果を公開投稿しないでください。関連する動作が変わった場合、このポリシーを更新します。
