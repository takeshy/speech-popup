version: '3'

includes:
  common: ../Taskfile.yml

tasks:
  # -H windowsgui keeps autostart from flashing a console window. The CLI
  # subcommands stay usable because main() calls AttachConsole first; see
  # console_windows.go.
  build:
    deps: [common:tidy, common:frontend]
    cmds:
      - task: syso
      - go build {{.BUILD_FLAGS}} -o {{.BIN_DIR}}/{{.APP_NAME}}.exe
      - cmd: powershell -NoProfile -Command "Remove-Item -ErrorAction SilentlyContinue wails_windows_*.syso"
        platforms: [windows]
      - cmd: rm -f wails_windows_*.syso
        platforms: [linux, darwin]
    vars:
      ARCH: '{{.ARCH | default ARCH}}'
      BUILD_FLAGS: '-tags production -trimpath -buildvcs=false -ldflags="-w -s -H windowsgui"'
    env:
      GOOS: windows
      GOARCH: '{{.ARCH}}'
      CGO_ENABLED: 0
  package:
    cmds:
      - task: msix
  syso:
    cmds:
      - wails3 generate syso -arch {{.ARCH}} -icon build/windows/icon.ico -manifest build/windows/wails.exe.manifest -info build/windows/info.json -out wails_windows_{{.ARCH}}.syso
    vars:
      ARCH: '{{.ARCH | default ARCH}}'
  msix:
    deps: [build]
    cmds:
      - powershell -NoProfile -ExecutionPolicy Bypass -File build/windows/msix/package.ps1 -Arch {{.ARCH}}
    vars:
      ARCH: '{{.ARCH | default ARCH}}'
  run:
    cmds:
      - '{{.BIN_DIR}}/{{.APP_NAME}}.exe'
